{
  "name": "@kowalski/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.ts",
    "test": "node --test src/**/*.test.ts"
  },
  "dependencies": {
    "@kowalski/types": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25"
  }
}
